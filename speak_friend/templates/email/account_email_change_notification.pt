 <html xmlns="http://www.w3.org/1999/xhtml"
       xmlns:tal="http://xml.zope.org/namespaces/tal"
       xmlns:metal="http://xml.zope.org/namespaces/metal">
    <div>
        <p>Your email has been changed on on <a href="${request.route_url('home')">${api.settings['site_name']}</a> from ${old_address} to ${new_address}.</p>
        <p>Please <a href="${request.route_url('contact_us')}">contact us</a> if you did not make the updates.</p>
    </div>
 </html>
